"""Legacy (pre-package) scripts.

This folder contains older one-off scripts kept for historical reference.
New development should use the importable package in `src/ccik/` and the
driver scripts in `scripts/`.
"""
